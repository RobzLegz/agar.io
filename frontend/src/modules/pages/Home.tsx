import React from "react";
import PageModule from "../PageModule";
import JoinContainer from "./../../ui/JoinContainer";

const Home = () => {
  return (
    <PageModule title="Agar.io" description="Agar.io, but bad">
      <JoinContainer />
    </PageModule>
  );
};

export default Home;
